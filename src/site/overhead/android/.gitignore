# Android/Gradle output and per-machine files for the overhead camera app.
# local.properties holds the SDK path of this machine only.
local.properties
.gradle/
.kotlin/
build/
app/build/
.idea/
*.iml
captures/
