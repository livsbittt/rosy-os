# products

Product profiles, not the shared runtime. `omx_adapter` is the manipulator profile (`device_type: manipulator`). It is disabled until a model, driver, mount, and payload are measured. Other arms are additional profiles, not new brand packages beside it.
