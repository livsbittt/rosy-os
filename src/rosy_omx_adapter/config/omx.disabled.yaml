# OMX is intentionally disabled until the model, driver, mount and payload are
# measured on Pinky Pro.  This file contains no fake hardware plugin.
omx:
  enabled: false
  model: ""
  driver_package: ""
  hardware_plugin: ""
  joint_names: [joint1, joint2, joint3, joint4, joint5, joint6]
  base_frame: base_link
  arm_base_frame: omx_base_link
  update_rate_hz: 100.0
